//2023-12-30,
// 1,2,3,4四个数能组成互不相同的3位数几个
#include <stdio.h>
int main()
{
 int i,j,k,sum=0;
	for(i=1;i<5;i++)
	{        
		for(j=1;j<5;j++)
		{
			for(k=1;k<5;k++)
			{
				if((i!=j)&&(i!=k)&&(j!=k))
				{
					sum+=1;
				}
			}
		}
		
	}
	printf("%d\n",sum);
   return 0;
}
